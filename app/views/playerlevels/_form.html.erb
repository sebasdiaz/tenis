<%= form_for(@playerlevel) do |f| %>
  <% if @playerlevel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playerlevel.errors.count, "error") %> prohibited this playerlevel from being saved:</h2>

      <ul>
      <% @playerlevel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player %><br />
    <%= f.text_field :player %>
  </div>
  <div class="field">
    <%= f.label :level %><br />
    <%= f.text_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
