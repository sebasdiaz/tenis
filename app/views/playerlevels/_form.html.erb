<%= form_for([@player, @playerlevel]) do |f| %>
  <% if @playerlevel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playerlevel.errors.count, "error") %> prohibited this playerlevel from being saved:</h2>

      <ul>
      <% @playerlevel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <h3><% @player.surname %>  </h3>
    <div class="field">
        Categorias:
        <%= f.collection_select :level_id, Level.all , :id, :name, :prompt => true %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
